{% if bearbeiteNews|length > 0 %}
    <div id="bearbeiteNeuigkeitDetail">
        <h3 class="marginLeft">Bearbeite Neuigkeit</h3>
        <form action="{{ route('bearbeite-neuigkeiten') }}" method="post" name="bearbeiteNeuigkeitDetailForm">
            <p>Titel:<br/><input type="text" name="titel" value="{{ bearbeiteNews.getTitel }}" required /></p>
            <p>Beschreibung der Neuigkeit:<br/>
                <textarea class="neuigkeitBeschreibung" name="text"
                          required>{{ bearbeiteNews.getText }}</textarea>
            </p>
            <p><input type="checkbox" name="loeschenNeuigkeit"/> Wollen Sie die Neuigkeit löschen?</p>
            <p><input type="submit" name="submit" value="Absenden"/></p>
            <input type="hidden" name="newsId" value="{{ bearbeiteNews.getNewsId }}" />
        </form>
    </div> <!-- #bearbeiteNeuigkeitDetail -->
{% endif %}